"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveProjectName = resolveProjectName;
const logger_1 = require("./logger");
const normalize_1 = require("./normalize");
function resolveProjectName(config) {
    const normalizedProjectName = (0, normalize_1.normalizePackageName)(config.name);
    if (normalizedProjectName.length < 1) {
        logger_1.logger.warn("Project name in 'federation.config.js' is empty, defaulting to 'shell' cache folder (could collide with other projects in the workspace).");
    }
    return normalizedProjectName.length < 1 ? 'shell' : normalizedProjectName;
}
//# sourceMappingURL=config-utils.js.map