import { Module } from './module';
import { UnassignedFileInfo } from '../file-info/unassigned-file-info';
import { FsPath } from '../file-info/fs-path';
import { FileInfo } from './file.info';
import { ModulePathMap } from './find-module-paths';
interface CreateModulesContext {
    entryFileInfo: UnassignedFileInfo;
    rootDir: FsPath;
    barrelFile: string;
}
export declare function createModules(modulePathMap: ModulePathMap, fileInfoMap: Map<FsPath, FileInfo>, getFileInfo: (path: FsPath) => FileInfo, { entryFileInfo, rootDir, barrelFile }: CreateModulesContext): Module[];
export {};
