"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.flattenModules = flattenModules;
const calc_tags_for_module_1 = require("../../tags/calc-tags-for-module");
function flattenModules(tagging, prefix = '') {
    let flattened = [];
    for (const [rawPath, value] of Object.entries(tagging)) {
        const path = rawPath.replace(calc_tags_for_module_1.PLACE_HOLDER_REGEX, '*');
        const fullPath = prefix ? `${prefix}/${path}` : path;
        if (typeof value === 'string' ||
            typeof value === 'function' ||
            Array.isArray(value)) {
            flattened.push(fullPath);
        }
        else {
            flattened = [...flattened, ...flattenModules(value, fullPath)];
        }
    }
    return flattened;
}
//# sourceMappingURL=flatten-modules.js.map