"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.tsConfig = tsConfig;
exports.fullTsConfig = fullTsConfig;
const defaultTsConfig = {
    compilerOptions: {
        target: "es2016",
        module: "commonjs",
        strict: true,
        skipLibCheck: true
    }
};
function tsConfig(config = {}) {
    return JSON.stringify({
        compilerOptions: Object.assign(Object.assign({}, defaultTsConfig.compilerOptions), config)
    });
}
function fullTsConfig(config = {}) {
    var _a;
    return JSON.stringify({
        extends: config.extends,
        compilerOptions: Object.assign(Object.assign({}, defaultTsConfig.compilerOptions), ((_a = config.compilerOptions) !== null && _a !== void 0 ? _a : {}))
    });
}
//# sourceMappingURL=ts-config.js.map