export interface TsConfig {
    baseUrl?: string;
    paths?: Record<string, string[]>;
}
export interface FullTsConfig {
    extends?: string;
    compilerOptions?: TsConfig;
}
export declare function tsConfig(config?: Partial<TsConfig>): string;
export declare function fullTsConfig(config?: Partial<FullTsConfig>): string;
