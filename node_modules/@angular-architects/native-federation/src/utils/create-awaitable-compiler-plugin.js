"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createAwaitableCompilerPlugin = createAwaitableCompilerPlugin;
const private_1 = require("@angular/build/private");
function createAwaitableCompilerPlugin(pluginOptions, styleOptions) {
    const originalPlugin = (0, private_1.createCompilerPlugin)(pluginOptions, styleOptions);
    let resolveDispose;
    const onDisposePromise = new Promise((resolve) => {
        resolveDispose = resolve;
    });
    const wrappedPlugin = {
        ...originalPlugin,
        setup(build) {
            // Wrap the build object to intercept onDispose
            const wrappedBuild = new Proxy(build, {
                get(target, prop) {
                    if (prop === 'onDispose') {
                        return (callback) => {
                            return target.onDispose(() => {
                                callback();
                                resolveDispose();
                            });
                        };
                    }
                    return target[prop];
                },
            });
            return originalPlugin.setup(wrappedBuild);
        },
    };
    return [wrappedPlugin, onDisposePromise];
}
//# sourceMappingURL=create-awaitable-compiler-plugin.js.map