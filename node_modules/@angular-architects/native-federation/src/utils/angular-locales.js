"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.shareAngularLocales = shareAngularLocales;
const build_1 = require("@softarc/native-federation/build");
function shareAngularLocales(keys, opts = {}) {
    if (!opts.config) {
        opts.config = {
            singleton: true,
            strictVersion: true,
            requiredVersion: 'auto',
        };
    }
    const ext = opts.legacy ? '.mjs' : '.js';
    return keys.reduce((acc, key) => {
        acc[`@angular/common/locales/${key}`] = {
            ...opts.config,
            packageInfo: opts.config.packageInfo || {
                ...opts.config.packageInfo,
                entryPoint: opts.config.packageInfo?.entryPoint ||
                    `node_modules/@angular/common/locales/${key}${ext}`,
            },
        };
        return (0, build_1.share)(acc);
    }, {});
}
//# sourceMappingURL=angular-locales.js.map