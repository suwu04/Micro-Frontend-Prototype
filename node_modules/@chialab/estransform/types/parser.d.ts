/**
 * Walk an ast node.
 * @param {Node} root
 * @param {Record<string, (node: Node) => void | Promise<void>>} visitors
 * @returns {Promise<void>}
 */
export function walk(root: Node, visitors: Record<string, (node: Node) => void | Promise<void>>): Promise<void>;
/**
 * @param {string} inputCode The code to parse.
 * @param {string} [filePath] The source file name.
 */
export function parse(inputCode: string, filePath?: string): Promise<{
    ast: any;
    comments: import("oxc-parser").Comment[];
    helpers: {
        /**
         * @param {number} start
         * @param {number} end
         * @returns {string}
         */
        substring(start: number, end: number): string;
        /**
         * @param {string} code
         * @param {number} [index]
         */
        prepend(code: string, index?: number): void;
        /**
         * @param {string} code
         * @param {number} [index]
         */
        append(code: string, index?: number): void;
        /**
         * @param {number} start
         * @param {number} end
         * @param {string} code
         */
        overwrite(start: number, end: number, code: string): void;
        isDirty(): boolean;
        /**
         * @param {{ sourcemap?: boolean|'inline'; sourcesContent?: boolean }} options
         */
        generate(options?: {
            sourcemap?: boolean | "inline";
            sourcesContent?: boolean;
        }): Promise<{
            code: string;
            map: any;
        }>;
    };
}>;
export type Node = {
    type: string;
    start: number;
    end: number;
    [key: string]: any;
};
